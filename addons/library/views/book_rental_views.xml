<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_library_book_rental_tree" model="ir.ui.view">
        <field name="name">library.book.rental.tree</field>
        <field name="model">library.book.rental</field>
        <field name="arch" type="xml">
            <list string="Book Rentals">
                <field name="book_id"/>
                <field name="partner_id"/>
                <field name="start_date"/>
                <field name="end_date"/>
            </list>
        </field>
    </record>

    <record id="view_library_book_rental_form" model="ir.ui.view">
        <field name="name">library.book.rental.form</field>
        <field name="model">library.book.rental</field>
        <field name="arch" type="xml">
            <form string="Book Rental Details">
                <sheet>
                    <group>
                        <field name="book_id"/>
                        <field name="partner_id"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_library_book_rental" model="ir.actions.act_window">
        <field name="name">Book Rentals</field>
        <field name="res_model">library.book.rental</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first book rental!
            </p>
        </field>
    </record>

    <menuitem
        id="menu_library_book_rental"
        name="Book Rentals"
        parent="library.menu_library_master_book"
        action="action_library_book_rental"
        sequence="40"/>

</odoo>
